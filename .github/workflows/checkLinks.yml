name: Check Links

on:
  push:
    tags:
      - '*'
  schedule:
    - cron: '0 6 * * 6'

jobs:
  linkinator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: JustinBeckwith/linkinator-action@v2.4.5
        with:
          paths: README.md CHANGELOG.md
